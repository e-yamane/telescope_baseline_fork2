runs:
  using: "Composite"
  steps:
    - name: Test with pytest
      run: |
        python -m pip install pytest pytest-html
        pytest tests --junitxml=report/pytest.xml --html=report/pytest.html
      shell: bash
